{
  "name": "coffeecode-ui-tests",
  "private": true,
  "description": "Playwright UI tests for the C0FFEEC0DE résumé landing page (served from dist/).",
  "scripts": {
    "build": "python3 ../../build/build.py --check",
    "install-browsers": "playwright install --with-deps chromium",
    "test": "playwright test",
    "test:full": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}